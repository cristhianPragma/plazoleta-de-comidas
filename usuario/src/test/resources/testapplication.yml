spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testes
    username: root
    password: 123
  application.name: user-service

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Plaza de comidas"
appVersion: "1.0.0"

